<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- Capture micro en arrière-plan : le diffuseur doit pouvoir quitter
         l'application sans couper son direct. Depuis Android 14 (API 34), un
         service de premier plan qui accède au micro doit déclarer ce type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- audio_service prend un wake lock dans enterPlayingState() : sans cette
         permission, le check lève une RemoteException, startForeground() n'est
         jamais appelé, et le système tue le process en arrière-plan (STR-109). -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Android 13+ : requise pour afficher la notification média (contrôles
         lecture / écran verrouillé) du service de premier plan. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="StreamPulse"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link : streampulse://app/reset-password?token=xxx -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="streampulse" android:host="app"/>
            </intent-filter>
        </activity>
        <!-- audio_service : service de premier plan + récepteur des boutons média
             (casque / écran verrouillé) pour la lecture en arrière-plan (STR-109). -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <!-- Service de premier plan du package `record`, qui maintient la capture
             micro quand l'application n'est plus au premier plan. Il n'est PAS
             déclaré par le plugin (son manifeste ne porte que la permission) :
             c'est à l'application de le faire, sinon `startService` échoue.

             `foregroundServiceType="microphone"` est ce qui autorise l'accès au
             micro hors premier plan depuis Android 10 ; sans lui, la capture
             rendrait du silence.

             `stopWithTask="true"` est la moitié « fermeture » du contrat : par
             défaut un service survit au balayage de l'application depuis les
             récents, et le micro continuerait de tourner alors que l'utilisateur
             croit avoir tout fermé. -->
        <service android:name="com.llfbandit.record.service.AudioRecordingService"
            android:foregroundServiceType="microphone"
            android:stopWithTask="true"
            android:exported="false"/>
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
